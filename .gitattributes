# Normalize line endings so Linux-based tooling (GitHub Actions, Binder) works reliably.
* text=auto

# Shell scripts must use LF (avoids "/bin/bash^M" errors).
*.sh text eol=lf
binder/start text eol=lf

# Build/config files are easiest to keep LF everywhere (Windows editors handle LF fine).
*.yml text eol=lf
*.yaml text eol=lf
*.qmd text eol=lf


